import LoadingLogo from "@/components/LoadingLogo";

export default function Loading() {
    return (
        <div className="flex items-center justify-center min-h-screen bg-pink-50/30">
            <LoadingLogo />
        </div>
    );
}
